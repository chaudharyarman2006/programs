#include <stdio.h>
int main() {
    int w;
    printf("enter w value:");
    scanf("%d",&w);
 if(w%2==0) {
    printf(" the number is even:");
    }
    else
    {
        printf(" the number is odd:");    
    }
}